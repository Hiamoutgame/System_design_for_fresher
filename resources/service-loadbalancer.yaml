apiVersion: v1
kind: Service
metadata:
  name: k8s-pod-loadbalancer
  labels:
    app: k8s-pod
spec:
  selector:
    app: k8s-pod
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
  type: LoadBalancer
